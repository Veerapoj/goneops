# {{PROJECT_NAME}} — {{ENV_NAME}} Sandbox

This project was generated by GoneOps Sandbox.

## Stack

- **Runtime**: Node.js + Express
- **Database**: PostgreSQL 15 (database: `{{DB_NAME}}`)
- **Cache**: Redis 7
- **Message Queue**: RabbitMQ 3.12

## Ports

| Service | Internal | External |
|---------|----------|----------|
| Web API | 8080 | {{WEB_PORT}} |
| PostgreSQL | 5432 | {{DB_PORT}} |
| Redis | 6379 | {{REDIS_PORT}} |
| RabbitMQ | 5672 | {{MQ_PORT}} |

## Credentials

| Resource | Username | Password |
|----------|----------|----------|
| PostgreSQL | {{DB_USER}} | {{DB_PASS}} |
| RabbitMQ | {{MQ_USER}} | {{MQ_PASS}} |

> **Security Note**: This is an MVP sandbox environment for local development only. Credentials are stored in plaintext and are not production-safe.

## Quick Start

```bash
npm install
npm run dev
```

Or with Docker:

```bash
docker compose up -d
```

## API Endpoints

| Method | Path | Description |
|--------|------|-------------|
| GET | `/health` | Health check |
| GET | `/api/test` | Connection tests for PostgreSQL, Redis, and RabbitMQ |

## Connection Strings

- **PostgreSQL**: `postgresql://{{DB_USER}}:{{DB_PASS}}@localhost:{{DB_PORT}}/{{DB_NAME}}`
- **Redis**: `redis://localhost:{{REDIS_PORT}}`
- **RabbitMQ**: `amqp://{{MQ_USER}}:{{MQ_PASS}}@localhost:{{MQ_PORT}}`
